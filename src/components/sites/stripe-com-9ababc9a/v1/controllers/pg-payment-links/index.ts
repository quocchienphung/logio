// Controller ports owned by the "pg-payment-links" group. Map keys are the reference's data-js-controller names.
import type { Controller } from "../types";

export const controllers: Record<string, Controller> = {};
