// Behaviours for the managed-payments page (HDS stack). Receives <main data-page="managed-payments">, returns a cleanup.
export function mount(root: HTMLElement): () => void {
  void root;
  return () => {};
}
